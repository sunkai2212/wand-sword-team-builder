# 技能图标圆环居中：全阶段完成快照

## 当前状态

- 已完成范围：一至七转，共 380 个技能图标。
- 本次新增完成：一至四转，共 236 个技能图标。
- 先前已完成：五至七转，共 144 个技能图标。
- 一至四转实现提交：`079ae1c`。
- 上一阶段快照 `2026-06-21-skill-icon-centering-stage-5-7-complete.md` 保留为历史记录，本快照取代其中的未完成状态。

## 统一标准

- 所有技能输出尺寸保持 `128 × 128`，文件名、技能顺序和界面代码不变。
- 全部 380 个技能均使用 `mask: "circle"`，圆形透明边界固定为输出尺寸的 `7/8`，即 112 px。
- 圆心固定在 `(64, 64)`，外框四周留白均为 8 px。
- 主动技能沿用已正确对准的原始中心，仅增加统一圆形边界。
- 一至四转被动技能按源图实际圆环重新缩放；二转被动技能另外修正了明显偏低的裁切中心。
- 圆外等级文字、白底和装饰被透明遮罩移除，不再进入技能方块。

## 视觉复核

- 使用 `npm run assets:audit-centering` 生成包含 380 张图标、中心十字线和 112 px 参考圈的审查页。
- 一至四转逐转复核后，圆形边界均与绿色参考圈重合，圆心与红色十字线对齐。
- 二转被动技能不再贴在方块上沿，图标主体回到中心。
- 五至七转在本次变更中保持不变；清单比对结果为一至四转 236 条变更、五至七转 0 条变更。

## 回归保护

- 素材脚本测试新增断言：目录中的 380 个技能必须全部使用统一圆形遮罩。
- 若以后新增技能却遗漏圆形边界，单元测试会直接失败。

## 验证证据

- `npm run assets:build`：成功重建 385 个目录素材。
- `npm run data:check`：380 个技能、5 个宠物、385 个素材通过。
- `npm test`：9 个测试文件、61/61 测试通过。
- `npm run build:pages`：TypeScript 与 GitHub Pages 子路径构建通过。
- `CI=1 npm run test:e2e`：38/38 Chromium 交互测试通过。
- `git diff --check`：通过。

## 后续恢复点

技能图标圆环居中任务已经覆盖全部一至七转。后续若发现单个图标的主体构图需要微调，应只修改该图标在 `data/source-assets.json` 中的裁切框并重新生成素材，不应在 CSS 中添加单图偏移。
