# 技能图标圆环居中：五至七转完成快照

## 当前状态

- 已完成范围：五、六、七转，共 144 个技能图标。
- 尚未处理：一至四转，共 236 个技能图标。
- 分支：`codex/skill-icon-centering`
- 工作树：`C:\Users\sunka\OneDrive\桌面\遗物数据库\.worktrees\skill-icon-centering`
- 基线审查提交：`fd94a00`
- 五、六转裁切提交：`bb6285e`
- 五至七转圆环标准化提交：`0849d58`

## 已完成内容

- 五、六转按职业、转数和主动/被动分组重新计算裁切框。
- 主动与被动技能不再共用同一裁切尺寸，小圆技能已放大到与大圆技能接近的视觉直径。
- 七转不再使用统一的 `{ left: 780, top: 900, width: 240, height: 240 }` 错误裁切框。
- 七转根据源图中的大圆和小圆使用两套裁切尺寸；特殊的大圆 `sage-s7-passive-5` 使用大圆规格。
- 素材生成器支持可选 `mask: "circle"`；五至七转启用直径为输出尺寸 `7/8` 的圆形透明遮罩，使圆环固定在 `128 × 128` 方块中心并去掉圆外等级文字与装饰。
- 新增 `npm run assets:audit-centering`，可生成包含全部 380 个技能、中心十字线和 112 px 参考圈的视觉审查页。

## 视觉复核

- 五至七转的圆形边界与 112 px 绿色参考圈基本重合。
- 圆心与 `(64, 64)` 十字线对齐，无整组明显偏移。
- 七转原先进入方块底部的等级文字已被圆形遮罩移除。
- 选择器、技能槽和导出 PNG 继续复用同一批 WebP，不需要额外 CSS 偏移。

## 验证证据

- `npm run assets:build`：成功重建 385 个目录素材。
- `npm run data:check`：380 个技能、5 个宠物、385 个素材通过。
- `npm test`：9 个测试文件、60/60 测试通过。
- `npm run build:pages`：TypeScript 与 GitHub Pages 子路径构建通过。
- `CI=1 npm run test:e2e`：38/38 Chromium 交互测试通过。
- `git diff --check`：通过。

## 后续继续方式

1. 一至四转仍保持原图，不应在当前完成范围中宣称已经统一。
2. 下次从计划的 Task 2 开始，复用同一审查页和圆形遮罩标准处理一至四转。
3. 一至四转完成后，再生成覆盖全部 380 个技能的最终完成快照。
