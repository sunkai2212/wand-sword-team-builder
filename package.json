{
  "name": "wand-sword-team-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run data:check && tsc --noEmit && vite build",
    "build:pages": "cross-env PAGES_BASE_PATH=/wand-sword-team-builder/ npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "assets:build": "node scripts/curate-assets.mjs",
    "data:check": "node scripts/validate-data.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^25.9.3",
    "cross-env": "^10.1.0",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
