# 任务 10 完成快照：最终验证、文档与静态交付

> 其中“仅支持站点根路径”的部署边界已于 2026-06-21 解除。当前线上部署状态以 `2026-06-21-github-pages-live.md` 为准。

## 恢复入口

- 工作树：`C:\Users\sunka\OneDrive\桌面\遗物数据库\.worktrees\team-builder`
- 分支：`feat/team-builder`
- 任务 9 基线：`eafc065`
- 任务 10 中断保存：`453b513`
- 任务 10 最终实现：`fc55f9a`（`fix: complete final delivery checks`）
- 本文件所在提交是任务 10 的最终文档快照。

## 已交付规则

- 全队共用一至七转；降低转数前确认，只清除超出新转数的技能。
- 5×4 站位盘可放置 1–4 名角色，允许重复职业，可移动、删除和更换职业。
- 每名角色有 4 个战技槽、4 个秘法槽和 1 个宠物槽；技能和宠物均可留空。
- 至少 1 名角色即可生成 PNG；空队才禁止生成。
- 技能仅显示图标，不显示技能名称、效果、数值或未经核验的说明。
- 五只宠物为：千明灵狗、暗夜帝王、虹彩星灵、竹林仙君、晨曦天使；允许重复选择。
- 页面显示非官方素材声明，声明不进入导出的 PNG。

## 任务 10 交付内容

- `README.md` 覆盖运行、测试、素材重建、生产构建和静态部署。
- README 使用与 Vite 8 一致的 Node.js 版本要求，并明确当前产物必须部署在站点根路径。
- 设计规格与实施计划已同步到“1–4 人、技能与宠物可留空、技能仅图标”的当前规则。
- 配置提示使用“还可添加 N 名角色”，避免暗示必须凑满四人。
- Playwright 改为通过 Vite API 管理测试服务，解决 Windows 上内置 `webServer` 清理后进程不退出的问题。
- 本地测试可复用已运行的 5173 开发服务器；CI 不复用，端口冲突时严格失败；只关闭本次测试创建的服务。

## 排错证据

- Playwright 无 webServer 的最小浏览器用例可正常退出。
- `npm run dev` webServer 与直接 Vite webServer 均在断言通过后超时，说明问题位于 Playwright 内置 webServer 生命周期，而非 npm 包装层。
- 外部 Vite 服务加无 webServer 配置可正常退出。
- Vite API global setup 定向用例可正常退出，因此采用该最小修复。
- 修复后分别验证了无预先服务、已有本地 Vite 服务和 `CI=1` 严格模式。

## 最终验证证据

- `npm run assets:build`：生成 385 个目录素材，未产生仓库漂移。
- `npm run data:check`：380 个技能、5 个宠物、385 个素材通过。
- `npm test`：8 个测试文件、54/54 单元测试通过。
- `npm run build`：数据校验、TypeScript 与 Vite 生产构建通过。
- `CI=1 npm run test:e2e`：38/38 浏览器测试通过，结束后 5173 无监听残留。
- `dist/` 静态预览：Chromium 可加载页面、选择转数并显示免责声明。
- `git diff --check`：通过。

## 独立复审

- 规格复审：最终通过；Critical / Important / Minor 均无遗留。
- 质量复审：最终 Ready — Yes；Critical / Important / Minor 均无遗留。
- 复审中发现并修复：少人阵容误导文案、已有开发服务复用、CI 严格端口语义和静态子路径部署说明。

## 部署与边界

- 发布 `dist/` 到静态托管服务的站点根路径。
- 运行时不需要后端、数据库、账号或环境变量。
- 当前资源使用 `/assets/...` 根路径；若未来需要 GitHub Pages 项目子路径部署，必须另行统一改造 Vite base 与目录资源路径。
- 内置浏览器连接曾因当前 Windows 沙箱权限失败；项目自带 Chromium 和生产静态预览均已完成验证，这不是产品运行问题。

## 后续状态

任务 1–10 均已完成。除非用户提出新需求，不再继续扩展功能；下一步只需决定如何整合 `feat/team-builder` 分支。
